# Variables para la aplicación y la base de datos
#
# Modo Docker Compose (recomendado para evaluación):
# - El servicio app usa `DB_HOST=db` y `DB_PORT=5432` internamente; no necesitas definirlos aquí.
# - Solo ajusta nombre de DB, usuario y contraseña para el contenedor de PostgreSQL.
#
# Modo local (sin Docker):
# - Define también `DB_HOST=localhost` y `DB_PORT=5432` (o los que uses).

# Base de datos (válido para Docker Compose y local)
DB_NAME=prueba_tecnica
DB_USER=prueba_user
DB_PASSWORD=prueba_pass

# Si lo ejecutas sin Docker, descomenta y ajusta:
# DB_HOST=localhost
# DB_PORT=5432

# Seguridad y límites
# Nota: EXPOSE_API_KEY_IN_UI=true solo para evaluación local; no usar en producción.
API_KEY=clave-demo-123
ALLOWED_ORIGINS=http://127.0.0.1:8000,http://localhost:8000
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=100
EXPOSE_API_KEY_IN_UI=true